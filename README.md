# У ході виконання даної лабораторної роботи було вивчено основні команди git:

* git rebase
* git clone
* git fetch
* git cherry-pick

# Також були створені власні виправлення у коді та були вирішені конфлікти.
